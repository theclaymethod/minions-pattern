interface:
  display_name: "Minions Pattern"
  short_description: "Delegate Claude and Codex work from a thin host"
  default_prompt: "Use $minions-pattern to delegate as much bounded execution as practical, keep the host as a thin accountable orchestrator, and use adversarial review."
